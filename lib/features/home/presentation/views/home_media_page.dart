import 'package:flutter/material.dart';

class HomeMediaPage extends StatelessWidget {
  const HomeMediaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
      body: const Center(
        child: Text('Home Media Page'),
      ),
    );
  }
}
